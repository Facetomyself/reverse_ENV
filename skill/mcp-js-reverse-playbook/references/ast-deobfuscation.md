# AST / JSVMP / WASM 反混淆路由

本文件只保留兼容入口，不再维护独立流程。

1. 先由 `mcp-js-reverse-playbook` 完成 request initiator、源码保存、runtime 采样和脱敏 fixture。
2. 确认 AST 混淆、JSVMP 或 WASM 边界成为核心阻塞后，读取 `D:\reverse_ENV\skill\web-deobfuscation\SKILL.md`。
3. 运行其只读 gate 分类，再按 evidence manifest validator 决定 L2、L3/partial 或 L4/triage-only。

不要在这里复制 transform、opcode 或 WASM 工作流；唯一事实源是 `skill/web-deobfuscation/`。
