---
name: ldplayer-control
description: Use for LDPlayer RE instance management in D:\reverse_ENV, including re-init, proxy setup, listing, destroy/reset flows, emulator isolation, adb checks, Frida readiness, and non-official LDPlayer automation validation.
---

This is a Codex repo-scope entrypoint for the existing project skill.

Before acting, read `D:\reverse_ENV\skill\ldplayer-control\SKILL.md` completely and use it as the source of truth. Resolve its relative `scripts/`, `references/`, and `templates/` paths against `D:\reverse_ENV\skill\ldplayer-control\`.

Do not duplicate or reinterpret the workflow here. If this entrypoint and the source skill disagree, the source skill wins and this wrapper should be updated.
