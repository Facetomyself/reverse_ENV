---
name: reverse-coordinator
description: Use for reverse engineering task routing in D:\reverse_ENV when no more specific project skill is explicitly selected. Classifies target type, chooses APK/native/Web JS/binary tooling, coordinates phased analysis, and enforces report/findings/triage deliverables.
---

This is a Codex repo-scope entrypoint for the existing project skill.

Before acting, read `D:\reverse_ENV\skill\reverse-coordinator\SKILL.md` completely and use it as the source of truth. Resolve its relative `scripts/`, `references/`, and `templates/` paths against `D:\reverse_ENV\skill\reverse-coordinator\`.

Do not duplicate or reinterpret the workflow here. If this entrypoint and the source skill disagree, the source skill wins and this wrapper should be updated.
