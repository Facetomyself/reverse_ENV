---
name: wmpf-offset-adaptation
description: Use for WMPF flue.dll Frida offset adaptation in D:\reverse_ENV, including missing addresses.{version}.json, version config 404, LoadStartHookOffset, CDPFilterHookOffset, SceneOffsets, and escalation to ida-reverse when automatic extraction fails.
---

This is a Codex repo-scope entrypoint for the existing project skill.

Before acting, read `D:\reverse_ENV\skill\wmpf-offset-adaptation\SKILL.md` completely and use it as the source of truth. Resolve its relative `scripts/` and `references/` paths against `D:\reverse_ENV\skill\wmpf-offset-adaptation\`.

Do not duplicate or reinterpret the workflow here. If this entrypoint and the source skill disagree, the source skill wins and this wrapper should be updated.
