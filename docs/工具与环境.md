# 工具依赖与环境

## 核心约束

所有工具安装在 `D:\reverse_ENV\` 内，不污染系统全局环境。

## 目录速查

| 类别 | 路径 |
|------|------|
| Python venv | `D:\reverse_ENV\.venv\` |
| JDK | `D:\reverse_ENV\tools\jdk\` |
| Node.js | `D:\reverse_ENV\tools\node\` |

## Android 逆向工具

| 工具 | 版本 | 路径 | 依赖 |
|------|------|------|------|
| `jadx` | 1.5.5 | `tools\jadx\bin\jadx.bat` | JDK 21 |
| `apktool` | 3.0.2 | `tools\apktool\apktool.bat` | JDK 21 |
| `frida` | 17.15.3 | `.venv\Scripts\frida.exe` | Python venv |
| `frida-tools` | 14.10.4 | `.venv\Scripts\frida-*.exe` (17 个) | Python venv |
| `adb` | 1.0.41 | `tools\adb\adb.exe` | — |

## 通用二进制分析

| 工具 | 版本 | 路径 |
|------|------|------|
| `radare2` | 6.1.8 | `tools\radare2\bin\radare2.exe` |
| `rabin2` | 6.1.8 | `tools\radare2\bin\rabin2.exe` |
| `rasm2` | 6.1.8 | `tools\radare2\bin\rasm2.exe` |
| `radiff2` | 6.1.8 | `tools\radare2\bin\radiff2.exe` |
| `rahash2` | 6.1.8 | `tools\radare2\bin\rahash2.exe` |
| `rax2` | 6.1.8 | `tools\radare2\bin\rax2.exe` |

## IDA Pro

| 项目 | 详情 |
|------|------|
| 版本 | IDA Pro 9.3 (9.3.260213) |
| 路径 | `resource\portable_win\` |
| 汉化 | `winmm.dll` (DLL劫持) + `cfg\language.txt` |
| 许可 | `idapro.hexlic` (至 2033-12-31) |
| idalib | `idalib.dll` + `idalib32.dll` |
| idapro wheel | v0.0.7 (`idalib\python\idapro-0.0.7-py3-none-any.whl`) |

## Java 运行时

| 项目 | 详情 |
|------|------|
| 发行版 | Eclipse Temurin JDK 21.0.11+10 LTS |
| 路径 | `tools\jdk\` |
| JAVA_HOME | 由 `tools\jadx-gui.cmd` 和 wrapper 脚本自动设置 |

## 雷电模拟器

| 项目 | 值 |
|------|-----|
| 版本 | LDPlayer 9 |
| CLI 实际路径 | `D:\leidian\LDPlayer9\ldconsole.exe` |
| ADB 实际路径 | `D:\leidian\LDPlayer9\adb.exe` |
| 安装壳路径 | `C:\Program Files\ldplayer9box\`（若存在，仅作为安装目录参考） |
| 后端 | VirtualBox 6.1.50 |
| ADB 映射 | `emulator-{5554 + index * 2}`，例如 index 1 为 `emulator-5556` |
| 管控脚本 | `tools\ldplayer\ldplayer.ps1` |
| 模板实例 | `re-base` / `re-xposed` / `re-stealth` |
| 模板备份 | `storage\ldplayer-backups\*.ldbk` |
| Android 模块资产 | `tools\android-modules\` |

大文件（~2GB），不纳入仓库。RE 实例管理优先使用 `skill\ldplayer-control\scripts\`，底层原子操作使用 `tools\ldplayer\ldplayer.ps1`。

当前 LDPlayer 采用模板实例模型：

| 模板 | 能力 | 用法 |
|------|------|------|
| `re-base` | Root + Kitsune Mask v27.0 + Frida 17.15.3 + mitmproxy CA | 低干扰动态调试、基础抓包 |
| `re-xposed` | `re-base` + LSPosed v1.9.2 + JustTrustMe v2 | 日常 App 逆向默认模板 |
| `re-stealth` | `re-xposed` + Hide My Applist V3.6.1 + Shamiko v0.7.5 | Root/模块隐藏要求更高的目标 |

真实目标 App 不直接装进模板。新项目用：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\reverse_ENV\skill\ldplayer-control\scripts\re-init.ps1" -Project myapp -Template re-xposed
```

模板 verified 备份位于 `storage\ldplayer-backups\`；APK/ZIP 模块本体位于 `tools\android-modules\` 并由 `.gitignore` 排除，只跟踪资产清单。


## Web 补环境隔离 runtime

| 项目 | 值 |
|------|-----|
| 项目主 Node | `D:\reverse_ENV\tools\node\node.exe`，用于 MCP 和默认 JS 工具链，不得替换或切换 |
| web-env 工具 | `D:\reverse_ENV\tools\web-env\` |
| 可选隔离 runtime | `D:\reverse_ENV\tools\web-env\runtimes\` 或 `D:\reverse_ENV\workspace\<项目名>\.runtime\` |
| xbs 参考仓库 | `D:\reverse_ENV\storage\xbsReverseSkill\`，不纳入 Git，默认只作为参考和纯 JS 检查器来源 |
| 隔离检查 | `powershell -File "D:\reverse_ENV\tools\web-env\check-isolation.ps1"` |

约束：Node 25/26、xbs native addon、魔改 isolated-vm、CycleTLS、impers、curl-cffi-node、curl_cffi、cyCronet 等补环境专用依赖不得安装进项目主 Node 或系统全局环境。缺依赖时先记录 native gap / TLS gap，用户确认后再安装到隔离 runtime。

## ruyipage 指纹浏览器

| 项目 | 值 |
|------|-----|
| 版本 | 1.2.43 |
| 浏览器 | Firefox 151.0a1（定制指纹浏览器） |
| 协议 | WebDriver BiDi（非 CDP） |
| 安装路径 | `C:\Users\mengma\AppData\Local\ruyipage\browsers\` |
| 用途 | 反检测浏览器自动化、过 Cloudflare/验证码 |

安装于系统 AppData（~200MB），不纳入本仓库。Python 包已装在 venv 内。

## ruyiTrace DOM 追踪

| 项目 | 值 |
|------|-----|
| 版本 | v1.2（Firefox 151 trace kernel） |
| 内核路径 | `tools\ruyitrace\firefox\firefox.exe` |
| 追踪机制 | `MOZ_DOM_TRACE=1` → C++ 层 Hook DOM API |
| 输出格式 | NDJSON |
| 分析脚本 | `tools\ruyitrace\trace_analyzer.py` |
| 大小 | ~340MB（C++ 层 Hook，页面脚本不可检测）

## Python venv 已安装包

| 包 | 版本 | 用途 |
|------|------|------|
| `frida` | 17.15.3 | 动态插桩核心库 |
| `frida-tools` | 14.10.4 | CLI 工具集 |
| `ruyipage` | 1.2.43 | Firefox 指纹浏览器自动化（WebDriver BiDi） |
| `angr` | 9.2.222 | 符号执行/二进制分析平台 |
| `qiling` | 1.4.6 | 跨平台二进制模拟 |
| `uncompyle6` | 3.9.3 | Python 字节码反编译 |
| `capstone` | 5.0.6 | 反汇编引擎 |
| `lief` | 0.17.6 | 二进制解析与修改 |
| `z3-solver` | 4.13.0 | 约束求解器 |
| `ida-multi-mcp` | 0.1.0 | IDA MCP 服务 |
| `idapro` | 0.0.7 | IDA headless 库 |
| `fastmcp` | ≥3.0.2 | jadx-ai-mcp 依赖 |
| `httpx` | ≥0.28.1 | jadx-ai-mcp 依赖 |

## First 微信小程序调试

| 项目 | 值 |
|------|-----|
| 版本 | 1.0.9 (开源版) |
| 仓库 | https://github.com/Spade-sec/First |
| 路径 | `tools\First\` |
| GUI 启动 | `powershell -File tools\First\first-gui.ps1` |
| CLI 启动 | `powershell -File tools\First\first-cli.ps1` |
| Python | `.venv\Scripts\python.exe` |
| 用途 | 微信小程序 Frida 注入 + CDP 代理调试 |
| 推荐微信 | 4.1.10 |

功能：Frida 注入微信 WMPF 进程 → CDP 代理桥接 Chrome DevTools → 云函数捕获、wx.* API 捕获、路由枚举导航、wxapkg 解密解包 + 敏感信息扫描、MCP Server 集成。

依赖：`frida>=17.0.0`, `websockets>=12.0`, `protobuf>=4.0.0`, `PySide6>=6.5.0`, `pycryptodome`（均已装在 venv）。

> **编译版存档**: v1.1.3 编译版（`First.exe` + `First-Windows.zip`）存于 `storage\First-release\`。当 WMPF 版本超过开源版覆盖范围时，用 `pyinstxtractor-ng` 提取新版 `addresses.{version}.json` 配置，复制到 `tools\First\frida\config\win\`。当前已额外补充 19977 / 19841 / 19871 / 19881 四个版本。

## reqable-mcp 抓包数据 MCP

| 项目 | 值 |
|------|-----|
| 版本 | 0.3.2 |
| 仓库 | https://github.com/ElonJask/reqable-mcp |
| 路径 | `tools\reqable-mcp\`（源码 + editable install） |
| CLI | `.venv\Scripts\reqable-mcp.exe mcp` |
| 用途 | 查询 Reqable 抓取的 HTTP/WebSocket 流量 — 请求搜索/域名统计/API 分析/代码生成 |

工作流：
1. 在 [Reqable](https://reqable.com) 桌面端配置 Report Server → `http://127.0.0.1:18765/report`
2. Reqable 抓包 → HAR 数据推送到 reqable-mcp
3. Claude Code 通过 MCP 工具查询分析流量（`reqable_*` 前缀，17 个 tools）

安装：`pip install -e tools\reqable-mcp`（已装在 venv，editable 模式）。

MCP 配置已写入 `.mcp.json`，前缀 `reqable_*`。

## 更新工具后

修改工具版本或路径后，必须同步更新：
1. 本文档
2. `CLAUDE.md` 的工具速查表
3. 相关 skill 的 fallback 路径
